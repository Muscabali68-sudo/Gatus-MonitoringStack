# name: Terraform Plan and Apply

# on:
#   workflow_run:
#     workflows:
#       - Build and Push
#     types:
#       - completed
#     branches:
#       - main 

# jobs:
#   terraform-deploy:
#     name: Terraform Plan and Apply

#     if: github.event.workflow_run.conclusion == 'success'
    
#     # Run the deployment commands on an Ubuntu runner
#     runs-on: ubuntu-24.04

#     # Allow up to one hour for planning and creating infrastructure
#     timeout-minutes: 60
    
#     permissions:
#     # Allow checkout to read the repository
#       contents: read

#     # Allow the job to request a GitHub OIDC identity token
#       id-token: write
#           # Allow only one infrastructure operation at a time
   
#     concurrency:
#       group: gatus-infra

#     # Let an active deployment finish
#       cancel-in-progress: false

#      # Run shell commands inside the main infrastructure folder
#     defaults:
#       run:
#         shell: bash
#         working-directory: Infra

#     # Tell Terraform it is running in automation
#     env:
#       TF_IN_AUTOMATION: "true"
    
    
#     steps:
#       # Download the exact commit that passed Terraform CI
#       - name: Check out validated code
#         uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
#         with:
#           ref: ${{ github.event.workflow_run.head_sha }}
#           persist-credentials: false 
      
#       - name: Download image tag
#         uses: actions/download-artifact@fa0a91b85d4f404e444e00e005971372dc801d16 # v4.1.8
#         with:
#           name: deployment-image-tag
#           run-id: ${{ github.event.workflow_run.id }}
#           github-token: ${{ secrets.GITHUB_TOKEN }}

#       # LÄS IN TAGGEN: Matchar din Terraform-variabel container_image_tag
#       - name: Read image tag
#         run: |
#           IMAGE_TAG=$(cat image-tag.txt)
#           echo "TF_VAR_container_image_tag=$IMAGE_TAG" >> "$GITHUB_ENV"


#      # Install the same Terraform version used by our checks
#       - name: Install Terraform
#         uses: hashicorp/setup-terraform@dfe3c3f87815947d99a8997f908cb6525fc44e9e # v4.0.1
#         with:
#           terraform_version: "1.15.6"
#           terraform_wrapper: false 

#       # Get temporary AWS credentials using our bootstrap deployment role
#       - name: Authenticate to AWS
#         uses: aws-actions/configure-aws-credentials@cbe3b392738ccf3f987d68400dafcf4b0624a56c # v6.2.4
#         with:
#           role-to-assume: arn:aws:iam::244306245751:role/github-actions-deployment-role
#           aws-region: eu-central-1
#           role-session-name: github-terraform-deploy

#       # Prepare Terraform and connect to the infrastructure state in S3
#       - name: Initialize Terraform
#         run: terraform init -input=false 

#       # Calculate infrastructure changes and save them for the apply step
#       - name: Terraform Plan
#         run: terraform plan -input=false -no-color -lock-timeout=5m -out=tfplan

#       # Apply the exact changes saved by the successful plan step
#       - name: Terraform Apply
#         run: terraform apply -input=false -no-color -lock-timeout=5m tfplan

name: Terraform Plan and Apply

on:
  workflow_dispatch: # Startar direkt när du klickar på knappen

jobs:
  terraform-deploy:
    name: Terraform Plan and Apply
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    
    permissions:
      contents: read
      id-token: write
   
    concurrency:
      group: gatus-infra
      cancel-in-progress: false

    defaults:
      run:
        shell: bash
        working-directory: Infra

    env:
      TF_IN_AUTOMATION: "true"
      AWS_REGION: eu-central-1
    
    steps:
      - name: Check out validated code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false 

      - name: Install Terraform
        uses: hashicorp/setup-terraform@dfe3c3f87815947d99a8997f908cb6525fc44e9e # v4.0.1
        with:
          terraform_version: "1.15.6"
          terraform_wrapper: false 

      - name: Authenticate to AWS
        uses: aws-actions/configure-aws-credentials@cbe3b392738ccf3f987d68400dafcf4b0624a56c # v6.2.4
        with:
          role-to-assume: arn:aws:iam::244306245751:role/github-actions-deployment-role
          aws-region: ${{ env.AWS_REGION }}
          role-session-name: github-terraform-deploy

      - name: Initialize Terraform
        run: terraform init -input=false 

      - name: Terraform Plan
        run: terraform plan -input=false -no-color -lock-timeout=5m -out=tfplan

      - name: Terraform Apply
        run: terraform apply -input=false -no-color -lock-timeout=5m tfplan
